{
  "name": "devmate-ai",
  "private": true,
  "scripts": {
    "dev": "concurrently -n frontend,backend -c blue,green \"npm --prefix frontend run dev -- --host 127.0.0.1\" \"npm --prefix backend run dev\"",
    "dev:frontend": "npm --prefix frontend run dev -- --host 127.0.0.1",
    "dev:backend": "npm --prefix backend run dev"
  },
  "dependencies": {
    "@monaco-editor/react": "^4.7.0",
    "axios": "^1.18.1",
    "highlight.js": "^11.11.1",
    "react-markdown": "^10.1.0",
    "react-router-dom": "^7.18.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
